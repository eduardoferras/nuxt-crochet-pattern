{
	"name": "crochet-pattern-monorepo",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"apps/*"
	],
	"scripts": {
		"changeset": "changeset",
		"version": "changeset version && npm install",
		"release": "changeset publish",
		"lint": "npm run lint --workspaces",
		"test": "npm run test --workspaces"
	},
	"devDependencies": {
		"@changesets/cli": "^2.29.7"
	}
}
